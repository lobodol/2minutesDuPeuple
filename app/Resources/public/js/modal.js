var Modal = {

    /**
     * Initialize event listeners & DOM elements.
     */
    init: function() {

    },

    /**
     * Display a message in a modal.
     *
     * @param {string} data : the string to display.
     */
    open: function(data) {
        alert(data);
    }
};