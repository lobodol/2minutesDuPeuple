nelmio_api_doc:
    documentation:
        info:
            title: L'API du peuple
            description: Une API de ouf pour les épisodes des 2 minutes du peuple
            version: 1.0.0
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
